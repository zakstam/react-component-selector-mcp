---
"@react-component-selector-mcp/react": minor
---

Allow component selection to work without an active server connection. The keyboard shortcut, UI button, and selection overlay now function regardless of connection status. Selections made while disconnected are queued and automatically sent when the connection is established.
